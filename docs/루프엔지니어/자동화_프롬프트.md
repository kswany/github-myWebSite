> Cursor 자동화 「매일 미니실험」에 붙여 넣는 프롬프트 원본입니다.  
> 규칙이 바뀌면 **이 파일을 먼저** 고치고, Cursor 대시보드 자동화 설정에도 같은 내용을 반영합니다.

kswany/github-myWebSite 저장소(main)에서 작업한다. 정적 GitHub Pages 사이트다.

규칙: docs/루프엔지니어/[2026-08-18] 일일_기능_규칙.md  
백로그: docs/루프엔지니어/[2026-08-18] 백로그.md

한 번에 짧은 웹 콘텐츠를 딱 1개만 완성한다. 게임일 필요는 없다. 퀴즈, 월드컵, 반응속도 테스트, 밸런스 선택, 작은 계산기처럼 브라우저에서 바로 쓰는 것이면 된다.

할 일:
1. 백로그의 미완료 1건을 고른다. 없으면 최근 한국 웹 트렌드로 아이디어 1개를 정해 백로그에 적은 뒤 만든다.
2. experiments/<영문-slug>/index.html 에 완성된 페이지를 만든다. 필요하면 같은 폴더에 CSS/JS를 둔다.
3. 루트 index.html 의 .grid 에 카드를 하나 추가한다. 기존 사주/그림 짝맞추기 카드와 같은 형식(kicker, h2, p, 열기/플레이)으로 한다.
4. experiments/index.html 에 같은 카드를 추가하고, js/nav.js EXPERIMENTS 에 slug·label 을 추가한다.
5. 백로그를 갱신한다(완료 표시, 다음 후보 1~2개 추가 가능).
6. saju/ 와 games/ 내용은 바꾸지 않는다. 홈 링크만 추가한다.
7. **main에 직접 푸시하지 말고, Pull Request를 연 뒤 main에 머지까지 반영한다.** draft PR이면 `gh pr ready` 후 `gh pr merge` 한다. PR만 열고 끝내면 턴 실패다.
8. HTML/CSS/JS만 사용한다. 서버, 로그인, 결제, API 키, 저작권 있는 브랜드/캐릭터는 금지. 한국어 UI, 쉬운 그림/버튼. 어려운 한자 금지.
9. 홈에서 한 번 클릭하면 실제로 동작하는 페이지가 나와야 한다. 머지 후 main 의 index.html .grid 에 새 카드가 있는지 확인한다.

사이트에 의미 있는 추가를 못 하면 PR을 열지 말고 이유를 적는다.

**턴 완료 기준:** 홈 카드 + experiments/index.html + nav.js + PR 생성 + **main 머지**. 하나라도 빠지면 미완성.
