> 최신 갱신: 2026-09-24
> 현행성 확인: 2026-09-24 (water-reminder 완료)

# 백로그

매일 자동화는 위에서부터 **아직 저장소·열린 PR에 없는** 미완료 1건만 처리한다. 같은 주제를 다시 만들지 않는다.

**턴 완료 = PR 생성 + main 머지 + 메인 `index.html` `.grid`에 카드.** PR만 열고 main에 안 올리면 미완성이다. `css/site.css`와 이미 있는 게임·실험 폴더는 수정하지 않는다. main에 직접 푸시하지 않는다 — PR을 머지한다.

## 대기

- [ ] 글자 크기 미리보기 — 본문 글자 크기(px/rem)를 바꿔 가며 줄 길이 체감
- [ ] BMI·체중 변화 — 키·몸무게로 BMI, 목표 체중까지 남은 기간 추정
- [ ] 집중 타이머 — 25분 작업·5분 휴식 포모도로, 브라우저만

## 완료

- [x] 사주 보기 (`saju/`)
- [x] 그림 짝맞추기 (`games/`)
- [x] 음식 월드컵 (`experiments/food-worldcup/`)
- [x] 반응속도 테스트 (`experiments/reaction-test/`) — 초록 신호에 클릭, 밀리초 기록
- [x] 밸런스 게임 (`experiments/balance-game/`) — A/B 여덟 문항, 비율 요약
- [x] 텍스트 비교 (`experiments/text-compare/`) — 좌우 붙여 넣기, 줄 단위 diff
- [x] 오늘의 하루 타입 (`experiments/mini-quiz/`) — 다섯 문항 퀴즈
- [x] 가벼운 계산기 (`experiments/quick-calc/`) — 디데이·팁 나누기
- [x] 글자 수 세기 (`experiments/char-count/`) — 공백 포함/제외, 줄 수, UTF-8 바이트
- [x] 원 그리기 (`experiments/circle-draw/`) — 캔버스에 원을 그리면 둥글기 점수
- [x] SQL 쿼리 도우미 (`experiments/sql-helper/`) — SELECT/INSERT/UPDATE/DELETE 뼈대, WHERE 조건
- [x] JSON 정리 (`experiments/json-formatter/`) — 들여쓰기 정렬·한 줄 압축·오류 위치 표시
- [x] 인코딩 도구 (`experiments/encode-tool/`) — Base64·URL 인코드·디코드, 한글 지원
- [x] 단위 변환 (`experiments/unit-converter/`) — 길이·무게·온도 즉시 변환
- [x] 제비뽑기 (`experiments/lottery-pick/`) — 이름 목록·숫자 범위에서 무작위 1개
- [x] 오늘의 한 줄 (`experiments/daily-line/`) — 위로·응원·휴식·웃음 분위기별 한 줄
- [x] 읽기 시간 (`experiments/reading-time/`) — 글 길이로 대략 몇 분 걸리는지 추정, 속도 선택
- [x] 색상 변환 (`experiments/color-converter/`) — HEX·RGB·HSL 상호 변환, 미리보기·복사
- [x] UUID 만들기 (`experiments/uuid-generator/`) — UUID v4 생성, 개수·대소문자 선택, 복사
- [x] 정규식 시험 (`experiments/regex-test/`) — 패턴·글 입력, g/i/m 옵션, 일치 하이라이트·목록
- [x] 유닉스 시간 변환 (`experiments/unix-time/`) — 초·밀리초↔날짜, 자동 단위, ISO·복사
- [x] 마크다운 미리보기 (`experiments/markdown-preview/`) — 입력 즉시 HTML 미리보기, 문법 치트시트, HTML 복사
- [x] 비밀번호 만들기 (`experiments/password-generator/`) — 길이·문자 종류 선택, 강도 표시, 보기/복사
- [x] Lorem 채우기 (`experiments/lorem-fill/`) — 글자 수·형식 선택, 더미 한글 문장 생성·복사
- [x] CSS 그라데이션 (`experiments/gradient-maker/`) — 두 색·각도 선택, 미리보기·linear-gradient 코드 복사
- [x] 대비율 확인 (`experiments/contrast-check/`) — 글색·배경색 WCAG 대비율·AA/AAA 통과 여부
- [x] QR 코드 만들기 (`experiments/qr-generator/`) — 글·주소 입력, 크기·색·오류 보정, PNG 저장
- [x] 슬러그 만들기 (`experiments/slug-maker/`) — 한글 제목→영문 slug, 구분 기호·복사, URL 미리보기
- [x] 해시 만들기 (`experiments/hash-maker/`) — SHA-256/384/512, UTF-8 바이트 표시, 대소문자·복사
- [x] 크론 설명 (`experiments/cron-explainer/`) — 5칸 cron 한국어 요약, 다음 실행 8회, 예시 프리셋
- [x] 타임존 변환 (`experiments/timezone-compare/`) — 도시 선택, UTC 오프셋·시차, 1초 갱신
- [x] 줄바꿈 정리 (`experiments/line-break-clean/`) — 빈 줄·공백 줄·줄 끝 공백, LF 통일, 복사
- [x] Base64 이미지 미리보기 (`experiments/data-url-preview/`) — data URL·Base64, 크기·MIME·용량·비율, 파일 읽기
- [x] 퍼센트 계산 (`experiments/percent-calc/`) — A/B 비율, 증감률, n% 금액, 자주 쓰는 비율 프리셋
- [x] 띄어쓰기 정리 (`experiments/space-normalize/`) — 연속 공백·탭 한 칸, 특수 공백 변환, 문단 줄 유지
- [x] 할인 역산 (`experiments/discount-calc/`) — 정가·할인율→결제·절약, 할인가 역산, 할인율 구하기
- [x] 사진 방향 확인 (`experiments/exif-orient/`) — JPEG EXIF 방향, 브라우저 vs 보정 미리보기
- [x] 문장 길이 나누기 (`experiments/sentence-wrap/`) — 목표 글자 수·공백·마침표 우선 줄바꿈, 문단 유지
- [x] 칼로리 간단 계산 (`experiments/calorie-calc/`) — 음식·분량 선택, 합계·하루 목표 대비 진행
- [x] 물 마시기 알림 (`experiments/water-reminder/`) — 목표 잔 수·간격 타이머·잔 기록, localStorage
